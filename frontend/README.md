# Companion Bot Frontend (Vite + React)

This project was bootstrapped with Vite and React.

## Getting Started

1. Install dependencies:
   npm install
2. Start the development server:
   npm run dev

The app will run on http://localhost:5173 by default.

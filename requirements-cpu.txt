# Web & API Framework
fastapi==0.128.2
uvicorn[standard]==0.40.0
pydantic==2.12.5
pydantic-settings==2.13.1
email-validator==2.3.0
python-multipart==0.0.22

# Database (Supabase PostgreSQL)
SQLAlchemy==2.0.46
psycopg2-binary==2.9.11

# Authentication & Security
PyJWT==2.8.0
passlib==1.7.4
bcrypt==5.0.0
python-dotenv==1.2.1

# AI & Bedrock (LangChain)
boto3==1.42.43
langchain==1.2.10
langchain-aws==1.2.5
langchain-community==0.4.1
langgraph==1.0.9

# Machine Learning & Audio Processing (CPU-only versions)
torch==2.10.0  # Will install CPU version automatically
transformers==5.1.0
pydub==0.25.1
websockets==12.0
wsproto==1.2.0
numpy<=2.3.0
soundfile
scipy

# HTTP Client
httpx

# Note: Removed heavy packages not needed for production:
# - openai-whisper (use transformers whisper instead)
# - av, sounddevice (not needed for API)
# - streamlit (only for UI, not needed for API)
